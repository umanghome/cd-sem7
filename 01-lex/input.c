#include <stdio.h>

int main () {
	// This is a single-line comment.
	/* This is a
		multiline comment */
	printf("//This is not a comment.\n"); // This is another comment.
	int a, b, c;
	b = 3;
	c = 3;
	a = b + c;

	return 0;
}